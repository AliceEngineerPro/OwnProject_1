import requestsimport jsonurl_list = [    # 'https://github.com',    'https://baidu.com',]proxy = {    'http': 'http://127.0.0.1:1080',}# def url_index(url):#     address = ''#     for index in url:#         address = index#     return addressresponse = requests.request(    method='GET',    # url=url_index(url_list)    url=url_list[0],    # proxies=proxy,    allow_redirects=True,    timeout=(500, 3000),    verify=True,)# print(response.text)print(response.headers, '\n', type(str(response.headers)))headers = json.loads(str(response.headers))print(headers)